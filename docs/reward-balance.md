# 보상 밸런스 설정

> 성장 책임 기준은 [성장 책임 기준](progression-responsibilities.md)을 따른다. 업적은 직접 전투 스탯 대신 수집 보상을 제공하며, 레벨·스탯 배분·장비·마스터리의 전투 책임을 혼합하지 않는다.

플레이어가 획득하거나 소비하는 수치는 `src/rewardBalanceConfig.js`가 단일 원본입니다.
경험치, 사냥터 전리품, 상자, 상인, 장비 성장과 환원, 업적, 숙련도 수치를 조정할 때는 이 파일만 수정합니다.

## 범위

- `experience`: 대전/사냥터 경험치, 레벨 비용, 레벨업 보상
- `rebirth`: 환생 후보 수·희귀 장비 기준 영구 기초 수치·액션/패시브 카드 장착·등급 상한·후보 가중치·불꽃 단계
- `hunting.chest`: 상자 비용, 파손 우선순위, 상자 보상 확률과 수량. 일반 상자만 파편 보상이 가능하고 고급 이상 상자는 장비를 확정 지급한다.
- `hunting.shards`: 전투 파편, 깊이 보정, 패배 보존, 전투 보상 배율
- `hunting.events`: 축복, 상인, 휴식, 상자 방, 저주 제단 등 이벤트 보상
- `equipment`: 장비 요구 레벨, 인벤토리, 강화, 판매, 분해, 융합, 기본·특수 옵션 수치와 특수 효과 쿨다운
- `progression`: 숙련도 조건과 효과, 업적 보상, 성장 보너스 상한

## 수정 원칙

기존 도메인 모듈은 보상 지급과 표시만 담당하고 수치의 원본을 소유하지 않습니다. 수치 변경 후에는 `npm test`, `npm run check`, `npm run format:check`, `node scripts/huntingUserScenario.mjs`를 실행해 보상 결과와 순환을 함께 확인합니다.

## 레벨 보상: 기본 스탯과 대표 행동 강화

레벨 보상은 캐릭터 ID별 `characterLevelProgressions`와 공통 `levelStatTargetMultiplier`에서 관리한다. Lv.2~10의 모든 행은 HP·공격·속도·방어력 중 하나 이상을 올리고, Lv.3·Lv.6·Lv.9 행은 대표 행동 tier를 추가한다. 기본 스탯은 사용자 스탯 배율보다 먼저 적용되며 장비 고정 수치는 그 뒤에 더한다. 수치형 행동 강화는 기준값 대비 5% 단위로 두고, 개수처럼 정수가 필요한 결과는 `Math.round`를 사용한다.

`levelStatTargetMultiplier`는 `1.5`다. 따라서 Lv.10의 HP·공격·속도·방어력 최종 수치는 각 캐릭터의 Lv.1 값의 정확히 150%다. 각 캐릭터의 50% 증가분을 Lv.2~10의 9개 구간에 누적 분배하며, 표시와 적용은 0.5 단위까지 보존한다. 쿨타임 스탯(`skill`)은 이 표의 기초 수치 대상이 아니며, 스탯 배분·장비·숙련도·패시브의 별도 modifier로만 조정한다.

| 구간 | 책임                                                                      |
| ---- | ------------------------------------------------------------------------- |
| Lv.3 | 대표 행동의 사용 감각이나 진입 조건을 바꾼다.                             |
| Lv.6 | 해당 행동이 전투 중 축적·선택·연계할 수 있는 중간 자원을 추가한다.        |
| Lv.9 | Lv.6 자원을 소비한 뒤 다음 교전으로 이어지는 제한된 후속 효과를 추가한다. |

아래 표는 Lv.3·Lv.6·Lv.9 대표 행동 tier의 구현 기준이다. `기준 → 결과`는 현재 능력의 기준값에서 강화 후 유효값을 함께 적는다. `게임 표시 문구`는 같은 보상 행의 `gameText`로 XP 결과, 다음 보상, 도감에 표시한다. 기초 수치의 레벨별 분배와 행동 강화 세부값은 모두 `rewardBalanceConfig`를 단일 수치 원본으로 사용한다.

Phantom은 표의 레벨 강화에 앞서 기본기를 단건 그림자 돌진으로 재구성한다. 기본 보정은 `속도 1.15×`, `공격 1.10×`, `방어 1.50×`, `충격 1.15×`이고, 기본 Shadow Strike의 추가 충돌 피해는 고정 18이 아니라 총공격력 `×1.50`이다.

| 캐릭터    | Lv.3                                                                                                                                 | Lv.6                                                                                                            | Lv.9                                                                                      | 게임 표시 문구                                                                                                              |
| --------- | ------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Archer    | 직접 조준 → 예측 조준: wind-up 동안 최신 위치·속도로 요격점을 계속 갱신하고 점선 원으로 표시. 발사 후 비유도                         | windup 20% 단축: `0.40초 → 0.32초` + 70% 단발 / 30% 연속 2발(0.12초 재조준)                                     | 50% 확률로 critChance × 2(최대 100%)의 독립 크리티컬 roll                                 | `예측 화살 + 속도 15%`<br>`조준 -20% · 70% 단발 30% 2발`<br>`50% 확률로 크리티컬 ×2`                                        |
| Orbit     | 같은 일제의 첫 적중점을 고정하고, 살아 있는 탄이 `0.15초` 곡선 보간으로 협공한다. 벽 반사 뒤에는 실제 반사 속도에서 같은 고정점으로 다시 잇는다. 첫 직접 ×0.80, 협공 직접 ×1.00 | 수명 `1.2→2.4초`, 일반 직접 ×0.90, 협공 직접 ×1.10, 실제 적중마다 70px 폭발 ×0.25                               | 미적중 비행 탄이 본체와 실제 충돌하면 원래 위성 슬롯을 한 번만 회수한다.                  | `첫 적중점 동기화 협공 · 직접 ×0.80/×1.00`<br>`수명 2.4초 · 협공 폭발 70px ×0.25`<br>`미적중 탄 본체 캐치 · 원래 위성 회수` |
| Trickster | 적이 씨앗을 밟으면 `0.50초` 20% 감속과 5틱 ×0.10 덩굴 피해를 준다.                                                                   | 적의 씨앗 접촉에 `1.8초` 표식. 첫 Seed Dash 적중이 기본 ×0.90과 별도로 ×1.20 폭발                               | 성공한 표식 폭발 접점에서 14초 후속 씨앗 1개를 산포한다. `0.50초` 뒤 충돌 활성화          | `덩굴 감속 0.5초 · 5틱 ×0.10`<br>`씨앗 표식 1.8초 · 돌진 폭발 ×1.20`<br>`폭발 접점 후속 씨앗 · 활성 유예 0.5초`             |
| Grenade   | 실제 이동 경로가 적대 몸체에 닿으면 로컬 접점에 붙는다. 대상별 동시 1개                                                              | 실제 폭발 피해 대상에게 0.5초, 0.1초 5틱 ×0.10 화상                                                             | 같은 소유자 점착 적 중 가장 가까운 표식을 향해 비점착 탄이 `0.50초` 뒤부터 최대 `2rad/s`로만 유도한다. 속도·퓨즈·추가 폭발 없음 | `실제 접촉 수류탄 점착 · 대상당 1개`<br>`폭발 피해 대상 화상 · 0.5초 ×0.50`<br>`점착 표식 유도 · 0.5초 뒤 2rad/s`            |
| Dash      | 실제 대시 적중 뒤 Dash 위치의 일시 레이저 캐스터가 0.35초 조준하고, 확정 각도를 유지한 채 Dash와 함께 이동하는 0.30초 레이저 구간 ×0.60 | 같은 레이저가 벽에서 1회 반사하고 입사·반사 구간이 각각 ×0.60                                                   | 같은 대상을 서로 다른 두 구간이 맞히면 종료 때 반경 100px ×1.00 교차 과부하               | `캐스터 조준 레이저 · 0.35초 조준 ×0.60`<br>`레이저 벽 1회 반사 · 구간별 ×0.60`<br>`교차 적중 과부하 · 100px ×1.00`           |
| Rage      | 충전 35% 이상: 점화 5틱 ×0.10 baseDamage                                                                                             | 충전 35-69% 점화, 70% 이상: 폭발 ×1.5 반경 120px                                                                | 충전 35-69% 점화, 70-99% 폭발, 100%: 여진 ×2.25 반경 180px + 방사형 impulse 1200~600      | `35%+ 점화 (5틱 ×0.10)`<br>`70%+ 폭발 ×1.50 120px`<br>`100% 여진 ×2.25 180px`                                               |
| Spin      | 만충 Crash 실제 피해를 유지한 뒤 `0.60초` 표면 절단 12틱 ×0.15, 합계 ×1.80을 더한다.                                                  | 절단 틱이 ×0.10에서 ×0.30까지 선형 증가해 합계 ×2.40이 된다.                                                    | 만충 전 340px 유체장으로 모든 적을 휘감고, 절단 12틱만 방어력을 무시한다.                 | `만충 Crash 유지 · 표면 절단 0.60초 · 12틱 ×0.15`<br>`가속 절삭 ×0.10→×0.30 · 합계 ×2.40`<br>`340px 관통 유체장 · 절단 방어 무시` |
| Eater     | 삼킨 중 소화 6틱 × 0.12 baseDamage                                                                                                   | 뱉기: ×1.0 피해 + 속도 ×3 + 반대 방향 impulse 420                                                               | 첫 유효 지형 충돌 시 파열: ×1.5 대상 / ×0.75 주변(반경 150px) + 반원 섬광                 | `삼킨 중 소화 6틱 ×0.12`<br>`뱉기 ×1.0 피해 · 속도 ×3 · 반동`<br>`첫 벽 충돌 파열 ×1.5+주변 ×0.75`                          |
| Bat Ball  | 타격 대상에게 기본 Wall Slam 단발 각충격의 `1.5배`를 스윙 방향으로 추가한다.                                                         | 타격점부터 첫 정적 충돌점까지 거리로 Wall Slam 최종 피해를 `×1.0~×2.0` 강화                                     | 유효 정적 충돌마다 본체 스킬 타이머를 초기화한다. 본체 기준 재발동 간격은 `0.50초`        | `회전 타구 · Wall Slam 각충격 ×1.5`<br>`비거리 HOME RUN · Wall Slam ×1.0~×2.0`<br>`벽 압박 적중 시 쿨다운 RESET`            |
| Vampire   | 첫 물기 뒤 생존. 실제 접촉마다 대상별 1초 간격으로 ×0.05 물기·실제 피해 70% 회복, 바깥 방향 240px/s 반동 뒤 0.15초 재유도 정지       | 개별 3.25~4.75초 수명 종료 시 반경 65px 적대 대상에게 박쥐당 ×0.05 폭발                                         | 물기 대상별 1초에 한 번 180px/s 견인·0.6초 혈흔. 최초 본체 충돌에서 ×0.15 파열 후 소모    | `반복 물기 ×0.05 · 반동 재돌입`<br>`수명 종료 폭발 65px · ×0.05`<br>`피의 견인 180px/s · 혈액 파열 ×0.15`                   |
| Gunner    | 모든 6~12발 연사의 마지막 탄이 해당 연사 피해 계수 `×2`                                                                              | 실제 회수한 일반 탄을 같은 계수로 현재 적 위치에 즉시 재사격. 첫 벽 반사에서 최근접 적 현재 위치로 한 번 재조준 | 일반 탄 실제 회수 +1, 20회에 8초 고정 포탑. HP 25%, 0.6초마다 총공격력 ×0.30              | `모든 6~12발 연사 마무리 탄 ×2`<br>`일반 탄 회수 즉시 재사격 · 첫 반사 재조준`<br>`일반 탄 20회 회수 · 8초 자동 포탑`       |
| Phantom   | 기본 돌진 적중 대상에 2.5초 표식을 남긴다. 표식 대상과의 다음 자연 적대 전투원 충돌에서만 추가 피해 없는 메아리 돌진을 1회 발동한다. | 표식 대상의 벽·지형 충돌도 메아리 돌진의 대체 발동 조건이 된다.                                                 | 메아리 돌진 적중 시 추가 피해 없는 종결 돌진을 1회만 허용하고, 이후 표식과 연계를 끝낸다. | `표식 대상 자연 충돌 시 메아리 돌진`<br>`표식 대상 벽·지형 충돌에도 메아리 돌진`<br>`메아리 적중 시 종결 돌진 1회`          |
| Hero      | 성장 코어에만 근거리 물리 자석 적용                                                                                                  | 실제 회수마다 최대 5 공명 조각, 다음 충돌에 0.1초 간격 ×0.20 순차 적중                                          | 정확히 5조각의 다섯 번째 적중에 90px ×0.75 Heroic Burst                                   | `성장 코어 근거리 자석 회수`<br>`회수 코어 공명 조각 · 순차 ×0.20`<br>`조각 5개 Heroic Burst · 90px ×0.75`                  |

Phantom은 표식의 자연 충돌과 벽·지형 충돌 중 먼저 발생한 하나만 메아리 돌진으로 바꾼다. 단계는 `기본 → 메아리 → 종결`에서 끝나며, 메아리와 종결에는 기본 돌진의 추가 능력 피해를 붙이지 않는다.

### Vampire Ball: 반복 물기 · 수명 종료 폭발 · 피의 견인

Vampire는 모든 레벨에서 4초마다 기본 속도의 `0.5배`로 박쥐 7마리를 발사한다. 각 박쥐의 생존 시간은 생성 시 `3.25~4.75초` 균등 무작위로 정하며 평균 4초다. 이 기본 수명 분산은 레벨 보상이 아니고, Lv.6 종료 폭발이 한 프레임에 겹치지 않게 한다. 박쥐 수·초기 속도·수명을 올리던 이전 Lv.3·Lv.6·Lv.9 수치 보상은 사용하지 않는다.

- Lv.3부터 박쥐는 첫 물기 뒤에도 살아남는다. 같은 적에게는 실제로 다시 접촉했을 때만 박쥐별 1초 간격으로 총공격력 `×0.05`를 주며, `takeDamage()`가 반환한 실제 피해의 70%를 한 번 회복한다. 물기 직후 접점 바깥 방향으로 `240px/s` 충격을 받고 `0.15초` 동안 유도를 멈춘 뒤 재추적한다.
- Lv.6부터 살아남은 박쥐는 개별 수명이 끝난 위치에서 한 번 소진된다. 반경 `65px` 안의 적대 볼·몬스터에 박쥐당 총공격력 `×0.05`를 주고 실제 피해의 70%를 회복한다. Vampire 본체와 아군은 대상이 아니다.
- Lv.9부터 실제 물기는 대상별 1초에 한 번 Vampire 방향 `180px/s` 단발 견인을 적용하고 `0.6초` 혈흔 취약을 남긴다. Vampire 본체의 최초 직접 충돌은 총공격력 `×0.15` 혈액 파열과 실제 피해 기반 흡혈을 한 번 적용하고 표식을 즉시 소모한다.

반복 물기·종료 폭발·혈액 파열은 각각 공통 투사체 방어(물기만), 크리티컬, 방어력, 남은 HP 상한을 한 번만 거친다. 7마리가 모두 4~5회 물고 폭발하는 이론 최대치는 약 총공격력 `×1.75~×2.10`이며 약 4초에 분산돼, 회복을 얻는 대신 순간 화력은 보수적으로 유지한다.

### Trickster Ball: 덩굴 · 표식 폭발 · 후속 씨앗

기본 `7초` 쿨다운, 씨앗 `3개`, 수명 `14초`, 주인 `1.55초·2.05배` 돌진과 기본 Seed Dash ×0.90은 레벨과 무관하게 유지한다. Lv.3부터 적대 대상이 씨앗을 밟으면 `0.50초` 동안 20% 감속되고 `0.10초` 간격으로 총공격력 ×0.10 피해를 5번 받는다. 주인이 자기 씨앗을 밟는 경우에는 기본 돌진만 발동한다.

Lv.6은 적대 씨앗 접촉에 `1.8초` 표식을 남긴다. 같은 대상에 이어진 첫 Seed Dash 적중은 표식을 먼저 소모하고 기본 ×0.90과 별도로 ×1.20 폭발을 한 번 준다. Lv.9은 그 폭발 접점에서 기본 수명 14초의 후속 씨앗을 즉시 산포하며, 생성 뒤 `0.50초` 동안 충돌하지 않는다. 후속 씨앗도 같은 규칙을 사용하므로 성공하면 다시 표식과 후속 씨앗으로 이어질 수 있다.

### Orbit Ball: 고정 접점 협공 · 폭발 · 본체 캐치

기본 위성 `5개`, 순차 재충전 `1초`, 완충 일제 간격 `0.18초`는 레벨과 무관하게 유지한다. Lv.3은 한 일제의 첫 유효 적중점만 저장하고, 아직 살아 있는 같은 일제 탄을 `0.15초` shortest-angle smoothstep으로 그 고정점에 보낸다. 협공 탄이 정적 표면에서 실제로 반사되면 반사 속도를 새 시작 방향으로 삼아 현재 위치에서 같은 고정점까지 다시 보간하며, 이동 대상을 다시 추적하지 않는다. 첫 직접 피해는 ×0.80, 협공 직접 피해는 ×1.00이다.

Lv.6은 비행 수명을 `1.2초`에서 `2.4초`로 늘리고 일반 직접 ×0.90, 협공 직접 ×1.10, 실제 적중마다 반경 `70px` ×0.25 폭발을 적용한다. 빗나간 고정점에는 폭발이 없다. Lv.9은 한 번도 적중하지 않은 비행 탄이 본체와 실제 충돌했을 때 원래 위성 슬롯을 한 번만 복구한다. 같은 프레임의 적중과 캐치가 겹치면 적중을 우선한다.

### Spin Ball: 표면 절단 · 관통 유체장

Spin Ball은 충돌 없는 시간으로 기준 `4.5초` 동안 회전 충전을 쌓고, 목표 각속도 `20πrad/s`, 즉 초당 10회전을 `applyTorque()`로 따라간다. 부분 충전 충돌은 기존 공통 접점 물리와 충전 소비 흐름을 유지한다.

Lv.3 이상 만충 직접 충돌은 일반 Crash 실제 피해를 한 번 유지하고, 한 대상의 실제 표면 앵커를 `0.60초` 유지한다. `0.05초`마다 총공격력 기준 12틱 ×0.15, 합계 ×1.80을 더한다. 종료 때는 고정 반동 대신 최초 충돌에서 공통 물리가 계산한 양쪽 분리 에너지를 한 번 지연 적용한다. Lv.6은 12틱을 ×0.10에서 ×0.30까지 선형 증가시켜 합계 ×2.40으로 바꾼다.

Lv.9 만충 중에는 반경 `340px` 안의 모든 적대 대상에 거리 smoothstep 감쇠, 접선 3:안쪽 1 방향, 최대 `420px/s²` 유체장을 적용한다. 첫 직접 충돌에서 유체장은 즉시 끝나고, 이어지는 절단 12틱만 방어력을 무시한다. 크리티컬과 HP 상한을 포함한 나머지 공통 피해 보정은 그대로 한 번씩 거친다.

### Bat Ball과 공통 Wall Slam

Bat Ball의 기본 160px 탐색, 120도 스윕, 3초 쿨다운, 직접 ×1.30, 넉백 550, Wall Slam `0.85초`는 레벨과 무관하게 유지한다. Lv.3은 기본 Wall Slam 단발 각충격의 `1.5배`를 실제 스윙 방향으로 추가한다. Lv.6 첫 정적 충돌은 타격점부터 충돌점까지 직선 거리로 `min(2, 1 + distance / hypot(width,height))` 배율을 최종 피해에 한 번 적용한다. Lv.9은 유효 정적 충돌마다 본체 스킬 타이머를 0으로 만들며, 본체 소유 `0.50초` 재발동 쿨다운을 둔다.

공통 Wall Slam 기본 피해 계수는 없다. 원본 유발자의 총공격력에 충격체의 충돌 직전 선형 속도, 접점 각속도, 정적 표면 법선 정렬을 결합하고 경기장 벽과 공통 장애물에서 동일한 contactPoint·normal을 사용한다. 피해는 공통 방어·크리티컬·HP 상한을 한 번만 통과하며, 같은 효과의 정적 충돌 재발동 간격은 `0.20초`다.

## 환생 보상

환생의 수치 원본은 `rebirth` 설정이다. 후보는 3장이고 영구 기초 수치 보상은 가중치 72, 액션 카드는 가중치 18, 패시브 카드는 가중치 10으로 같은 공용 풀에서 비복원 추첨한다. 후보 안의 보상 ID는 중복하지 않는다.

- 영구 기초 수치 보상은 HP·공격·속도·방어력 중 서로 다른 두 항목을 무작위로 골라 즉시 누적한다. 각 수치는 `equipment.statRanges.rare`의 기본 범위에 같은 `equipment.statValueRatios`를 곱해 만들므로 희귀 장비의 가치 기준을 그대로 따른다. 후보를 만들 때 선택된 두 수치와 식별자를 함께 저장해, 새로고침 뒤에도 다시 추첨하거나 재계산하지 않는다. `radius`, `mass`, 충돌·이동 판정과 쿨타임은 포함하지 않으며, 카드 rank·장착 한도와 무관하다.
- 액션·패시브 카드의 rank와 장착 한도는 각각 10, 3이다. 중복 카드는 rank만 1 올린다.
- 액션 카드는 원래 Ability 인스턴스를 `abilityTier: 0`으로 생성하므로 해당 캐릭터의 XP 대표 행동 tier를 상속하지 않는다. 발생 Ability가 보유한 rank modifier만 적용한다.
- 패시브 `냉각 회로`는 rank 1에서 모든 능력 쿨타임을 30% 줄이고, rank마다 2%p씩 강화해 rank 10에서 48% 감소를 상한으로 한다. 이 감소는 스탯 배분·숙련도·장비 쿨타임 보정 뒤에 곱한다.
- 불꽃 visual stage는 누적 환생 횟수만 입력으로 사용한다. 사각 셀 밀도는 환생 1회의 56개와 10환생의 최종 92개만 정의하고, 중간 환생은 정수 반올림 선형 보간한다. 셀의 크기·투명도·난류 흔들림도 같은 1~10 구간을 선형으로 보간하므로, 시각 stage 경계에서 강도가 튀지 않는다. `WeakMap` 플룸은 새 셀을 공 외곽 전체의 독립 표면 앵커에 강하게 결합했다가 수명에 따라 결합을 푼다. 새 셀의 초기 속도는 해당 앵커의 외곽 법선 방향이고, 위쪽 부력은 매 프레임 작용하면서 수명에 따라 강해져 초기 궤적을 위로 굽힌다. 결합이 풀린 셀은 공통 후류 방향으로 강제 합류하지 않고 독립적으로 위로 타오르며, 공의 실제 이동과 남은 이전 위치가 자연스러운 후류를 만든다. 곡선 윤곽이나 외곽 점선은 그리지 않는다. 셀은 수명에 따라 밝은 중심·주황 몸통·적색 끝으로 바뀌며 `source-over` 알파 합성으로 겹친다. 물리 수치·저장 데이터는 바꾸지 않는다.

같은 220px/s 정면 충돌에서 만충 접점 회전 속도는 약 `3140px/s`다. 피해 속도는 `220 + 3140 = 3360px/s`로 환산되며, 현재 스핀볼 기본 수치·방향 조건의 충돌 피해는 기존 보너스 방식 `4 → 6` 대신 선형 속도 환산 방식 `4 → 67`이 된다.

### Hero Ball: 성장 코어 · 공명 · Heroic Burst

Hero는 자동 오브를 발사하지 않고 1초마다 최대 5 성장 스택을 충전한다. 적대 전투원과 실제 충돌하면 스택을 모두 소비해 접점에서 같은 수의 성장 코어를 무작위 방향으로 방출한다. 코어는 8초 유지, 생성 직후 0.16초 직접 회수 유예, 소유자별 활성 5개 상한을 사용한다. 속도는 소유자의 현재 전투 속도 `×0.72~0.96`이고, 상한을 넘기면 가장 오래된 활성 코어를 제거한다.

코어는 HP·공격·속도·방어·쿨타임·치명타 중 하나를 2~6 올린다. 치명타는 `%p` 단위이며 100%에서 멈춘다. 모든 스탯 적용은 Hero 본체와 코어가 실제로 충돌했을 때만 발생한다.

| 구간 | 강화         | 수치와 판정                                                                                                                              |
| ---- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| Lv.3 | 코어 자석    | `Hero 반지름 × 3.5 + Core 반지름` 안의 성장 코어만 기존 물리 자석으로 가속한다. 순간 이동·전역 회수는 없다.                              |
| Lv.6 | 공명 조각    | 실제 적용된 코어마다 속성색 조각을 최대 5개 저장한다. 다음 적대 충돌에서 같은 대상에 0.1초 간격으로 순차 적중해 조각마다 총공격력 ×0.20. |
| Lv.9 | Heroic Burst | 정확히 5개 조각의 다섯 번째 적중에서 한 번만 발동한다. 대상 포함 반경 90px 적대 대상에 ×0.75와 기본 충돌 넉백 60%.                       |

## 숙련도 수치

숙련도 등급 수치는 `progression.masteryTiers`가 단독 소유한다. 동일 키에 대한 누적 상한은 없으며, 여러 캐릭터가 같은 키를 제공하면 단순 가산된다. 캐릭터별 정의나 전투 코드는 수치를 소유하지 않는다.

숙련도는 장비의 고정 수치까지 합친 fighter spec에 마지막으로 퍼센트 적용한다. 충돌 피해 증감도 장비 충돌 보정과 액션 처리 뒤의 최종 피해 단계에서 적용한다. 따라서 장비와 숙련도의 수치를 조정할 때는 서로의 효과가 곱해지는 순서를 바꾸지 않는다.

수치 변경 전후에는 능력이 없는 표준 볼로 같은 시드와 좌우 시작 위치를 교차 비교한다. GOLD 단일 효과의 목표는 일반 스탯·충돌·회복 계열에서 대체로 `+3~5%p` 승률 변화다. 속도 복귀율과 쿨다운은 승률 외에 HP 잔량과 능력 회전율도 함께 본다.

갈증은 `masteryRuntime.vampire`의 4초 준비 시간과 최대 결손 HP 배율 2를 사용한다. 회복식은 `실제 충돌 피해 × 등급 흡혈률 × (1 + 결손 HP 비율)`이며, 표준 충돌과 고피해 충돌의 회복 격차를 함께 측정한다.

## 장비 스탯 환산

`scripts/equipmentStatBalance.mjs`는 능력이 없는 표준 볼(HP 100, 공격력 10, 방어력 1, 속도 300)로 장비형 고정 스탯의 상대 가치를 측정합니다. 같은 시드를 좌우 시작 위치에 교차 적용하고, 장비 없음 전투와의 승률 상승폭만 계산해 시작 위치 편향을 제거합니다. 제한 시간 안에 끝나지 않은 전투는 최대 HP 대비 남은 HP 비율로 판정해 표본에서 빠지지 않게 합니다.

기본 실행은 `node scripts/equipmentStatBalance.mjs`입니다. 정밀 측정은 PowerShell에서 `$env:BALANCE_SAMPLES=120; node scripts/equipmentStatBalance.mjs`처럼 표본 수를 늘려 실행합니다.

장비 기본 옵션은 등급별 `statRanges`의 숫자를 공통 가치 포인트로 사용한다. 실제 고정 스탯은 `equipment.statValueRatios`를 곱해 생성하며, 현재 기준은 HP 10, 공격력 1, 방어력 1, 속도 5다. 이 비율은 장비 스탯 부여의 유일한 기준값이며, common 장비의 1~3포인트 옵션은 HP +10~30, 공격 +1~3, 방어 +1~3, 속도 +5~15 중 하나가 된다.

새 장비명은 가장 높은 가치 단위의 기본 스탯 하나를 대표 접두사로 사용한다. HP는 `튼튼한` 계열, 공격은 `맹공의` 계열, 방어는 `수호자의` 계열, 속도는 `질풍의` 계열을 베이스 장비명 앞에 붙인다. 특수 옵션이 있으면 이름과 구분되는 점 접미사로 붙인다. 예: `질풍의 철검 • 갈망`.

## 장비 특수 옵션

현재 특수 옵션은 같은 종류를 여러 장비에서 얻어도 가장 높은 수치 하나만 적용한다. 서로 다른 종류는 함께 적용한다.

- `파쇄`: 적대 전투원 간 충돌 거래에서 자신이 주는 모든 실제 충돌 피해의 원본 수치를 증가시킨다. 기본 충돌과 충돌 처리 중 발생한 접촉형 스킬 피해가 대상이다.
- `순환`: 장착자의 스킬 쿨다운을 비율만큼 줄인다. 스킬 스탯과 숙련도 쿨다운 보정 뒤에 함께 곱해진다.
- `갈망`: 적대 전투원 간 한 번의 충돌 거래가 끝난 뒤, 기본 충돌과 접촉형 스킬로 준 실제 총 피해의 비율만큼 회복한다. HP가 부족하고 장비 효과 쿨다운이 준비됐을 때만 발동하며, 현재 쿨다운은 `2.5초`다.
- `중량`: 실제 질량을 증가시켜 rigid-body 충돌과 회전 관성 결과를 바꾼다. Gunner 숙련도의 중량 장전과 곱으로 결합된다.
- `반향`: 벽 반사 뒤 법선 속도를 비율만큼 증가시킨다. Orbit 숙련도의 반사 궤도와 곱으로 결합된다.
- `소용돌이`: collision solver가 소유자에게 전달하는 각충격을 비율만큼 증가시킨다.

특수 옵션 변경은 `src/rewardBalanceConfig.js`에서 수치만 조정하고, 충돌 거래 기준이 유지되는지 회귀 테스트와 시뮬레이션으로 확인한다.
